# Confirmation Log

| 确认项 | 原始草案 | 用户选择/确认 | 是否写入最终 Skill | 备注 |
| --- | --- | --- | --- | --- |
| 业务目标 | 根据股票代码、被套深度、仓位生成解套方案 | 用户确认保留 | 是 | 保留原 DSL 的“解套方案”业务表达 |
| 目标 Skill 名称 | `investoday-ai-unwind-advisor` / AI解套顾问 | 用户确认保留语气 | 是 | 展示标题恢复为“AI解套顾问” |
| 输入字段 | 股票代码、被套深度、仓位占比 | 用户确认使用新字段 | 是 | 正式字段使用 `position_ratio`，原 DSL 变量 `set_postion` 仅作溯源记录 |
| 工具映射 | 常规接口走 `investoday-api`，ETF 两接口走 `call_api.js` | 用户确认 Key 分工 | 是 | 所有接口统一使用 `INVESTODAY_API_KEY`。ETF 轮动策略和 ETF 入选理由生成依赖 `api.json` path，不依赖 `investoday-finance-data` references，并通过 `node scripts/call_api.js` 调用 |
| 数据获取流程 | 技术面、基本面、信息面、赛道、波段、换股/ETF替代 | 用户确认按新接口口径执行 | 是 | 解套信号统计/明细与其他接口一样统一使用 `INVESTODAY_API_KEY`；单个接口异常不影响其他接口调用 |
| 输出格式 | 解套方案 Markdown 报告 | 用户确认保留 | 是 | 当前保留原 DSL 章节结构，并加入免责声明、证据约束和数据不足规则 |
| 是否生成正式 Skill | 已生成正式 Skill 文档 | 用户确认继续维护 | 是 | 当前保留在转换输出目录，未执行挂载/发布 |
| DSL 策略规则 | 被套/仓位阈值、波段潜力阈值、七类方案分支 | 待确认 | 否 | 已写入草稿，但正式版需确认是否保留原方案排序 |
| 输出语气 | 解套方案/主推策略 | 用户确认保留 | 是 | 保留原 DSL 语气，同时保留免责声明和证据约束 |
| 原 DSL 可运行性 | 原 DSL 在目标 Dify 环境是否可运行 | 用户已确认可运行 | 是 | 本地 validator 失败仅记录为本地插件/依赖基线不一致 |
| 挂载/发布 | 是否复制到 `~/.skills-manager/skills` 或发布 | 用户确认不动 | 是 | 当前只保留在转换输出目录 |
