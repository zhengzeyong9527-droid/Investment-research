# 变更日志

## Unreleased

- 强化成长大师报告稳定性输出契约：轻量版固定为结论、核心判断、三条理由、主要风险、反证信号和合规说明，并要求核心段落使用“判断句 + 数据证据 + 方法论解释”。
- 收口公司类型为单一标准枚举：用户可见公司类型字段只能输出缓慢增长型、稳健增长型、快速增长型、困境反转型、周期型、隐蔽资产型之一，禁止稳定增长型、成熟稳健型、资产机会型、多类型组合和自造类型。
- 增加输出前质量自检要求：检查明确结论、核心判断表、关键数据支撑、反证信号、历史事实/预测样本/方法论推断区分、PEG 口径解释和交易化禁语。
- 增加 WorkBuddy 风格轻量版片段参考，并补充根 `SKILL.md` 与触达 Markdown 文件需保持 UTF-8 without BOM 的导入兼容要求。
- 调整轻量版用户可见数据口径：标题下只列实际取得的行情估值、财务报告期和机构预测样本等分句；未展开或未取得实际值的行业估值、PEG 三情景、成长股特征不再输出“暂不展示”“暂无可用数据”等占位话术。
- 调整普通单股轻量版数据范围：`summary_fast` 扩展为五槽位，新增近期机构预测样本摘要；轻量版默认获取预测评级紧凑标准输出，但不生成完整盈利预测表、研报依据表或评级变动原因。
- 收口轻量版用户可见缺口话术：不再输出流程化占位；轻量版缺少事实或未展开深链路时，改为省略对应分句、降低置信度或通过下一步入口引导深度分析。
- 优化 Windows / PowerShell 下 `node scripts/gs-api.js` POST 取数：公共请求脚本新增 `--body key=value ...`，由脚本自动生成合法 JSON，并对 `.ps1` 调用分支转义 `--body-json`，避免 shell 拆分引号导致反复失败。
- 优化普通单股轻量版性能：默认改为 `summary_fast` 四块快速画像，只读取 `references/light-fast-summary.md` 和轻量输出/合规契约，不再读取 `01-06` 完整维度 reference；只取标的识别、行情估值、核心财务、行业归属和必要行业景气摘要。
- 新增父级紧凑调用约束：行业、财务、估值子 skill 增加或强化 `compact_parent_summary`，避免普通轻量版触发 8 维行业深度、完整 5 年财务包、完整估值/PEG/DCF 链路。
- 将盈利预测、PEG 三情景、评级变动、行业估值深链路和成长股特征深层证据改为完整报告、单维度或用户明确追问时触发。
- 收敛研报查询默认体量：股票预测评级默认 `pageSize=20`，预测评级变动接口仅在明确询问变化原因时调用，避免轻量版默认触发大体量研报返回。
- 修复 Windows 公共请求脚本兼容性：`scripts/gs-api.js` 支持发现并调用 `node scripts/gs-api.js`。
- 引入全局最小读取与 `dimension_plan` 调度：新增 `references/core-workflow.md` 和 `references/dimensions/index.md`，普通轻量版改为 `summary_fast` 四块快速画像，完整分析才展开 full 级六维模板。
- 移除包内子 Skill 的 frontmatter 前置依赖声明：行业、财务、估值、研报和成长股特征能力保留在 `skills/`，运行时按 `dimension_plan` 按需读取，不再写入 `requirements.skills` 或 `metadata.clawdbot.requires.skills`。
- 新增行业估值日期输出硬门禁：单股轻量版、完整报告、PEG 单维度和成长股特征单维度在写“行业估值截至未返回”前，必须先确认已走完稳定对比行业到同体系二级行业、再到行业指数估值最近交易日的补充链路；只取得行业分位或行情统计不得判定行业估值未返回。
- 强化企业盈利预测研报依据硬门禁：完整报告或单维盈利预测只要输出未来预测、三情景预测或机构预测样本，必须输出 `日期 / 研报标题 / 发布机构 / 用途` 四列表格；来源字段缺失时保留占位，不得省略研报依据。
- 修复行业基准 PEG 状态误判：行业指数估值已返回但行业 PE 为负或为 0 时，必须写“不适用”而不是“未返回”，并继续尝试同体系一级行业回退；一级回退可计算时必须展示“一级回退口径”的行业基准 PEG。
- 补强成长股特征单维度估值链路：即使不展开完整 PEG 报告，也必须通过估值分析紧凑标准输出补齐个股行情估值日期、稳定对比行业和行业估值日期；已能解析行业指数估值时不得输出“行业估值截至未返回”。
- 优化行业生命周期评估输出格式：保留原 `结论摘要 / 判断依据 / 主要风险 / 成长大师视角` 四段结构，结论摘要保持字段式输出，增长阶段特征改为固定证据表，单维度调用与完整报告均不得摘要化。
- 修正行业 PEG 同口径规则：PEG 行业对照固定优先二级行业，二级行业无法形成有效行业基准 PEG 时才回退一级行业；输出必须展示行业名称和必要的“一级回退口径”说明。
- 行业 PEG 默认改为单一“行业基准 PEG”，不再用不同期限字段拼接行业侧三档 PEG；只有底层明确返回同一预测期限下的高 / 中 / 低预测情景时，才允许额外输出行业侧三档 PEG。
- 区分行业景气度硬评分场景：个股模式保留“机构评级趋势”并使用 5 分制；行业直问去掉该评分项，固定使用 ROE、毛利率、净利率三项 4 分制，不得用代表股、研报热度或行业预测增速替代。
- 完善研报预测修正链路：机构评级趋势、预测上调占比和成长股特征中的预测样本变化统一消费研报查询原子 skill 的预测修正摘要；可比样本可得时必须输出样本数、上调数和上调占比，可比样本不足时必须说明具体缺口。
- 收口用户可见方法论名称：最终报告禁止出现历史人物相关口径、历史英文称呼、历史人物式表述或历史 slug，统一改写为“成长大师”或“成长投资方法论”。
- 修复行业景气度输出校验：`总评分` 必须与 `各项计分` 分项求和一致，发现矛盾时按分项结果重算。
- 完善行业 PEG 数据链路：行业 PE/PB/PS 五年分位不得替代当前行业 PE；必须继续通过二级行业优先、一级回退链路解析行业指数并查询行业指数估值，失败时说明具体缺失项。
- 调整用户可见免责声明：将“以下内容”改为“以上内容”，避免报告结尾语义不顺。
- 优化完整报告输出排版：企业盈利预测的研报依据改为逐条表格展示，避免多个来源堆成一整段。
- 优化行业景气度输出契约：核心景气指标统一按百分数展示并带 `%`，新增评分口径说明表，且数据已取得但未达阈值时不得误写为数据不足。
- 优化成长股特征匹配输出：综合评级仍按 10 项状态内部计算，但用户可见只展示评级依据，不再展示总分、状态数量或计算式。
- 调整单标的标题模板：完整报告统一为“成长大师视角：股票名称（代码）成长分析”，轻量版统一为“成长大师视角：股票名称（代码）成长简析”。
- 调整用户可见数据口径：报告标题下统一写“仅基于 gs 数据”，并列示行情估值日期、行业估值日期、财务报告期和机构预测样本口径；不再写未经当前链路校验的投资者关系、年报、季报等资料参考清单。
- 强化数据口径占位符校验：行情估值、行业估值和财务报告期只在取得实际返回值时输出，缺失时省略对应分句，不得把 `xxxx`、`YYYY`、括号占位符或缺口占位话术原样输出给用户。
- 补齐 PEG 行业对照数据链路：行业 PEG 必须先按二级行业优先取得行业指数 PE，再结合同口径行业基准预测增速计算；未尝试行业指数估值和行业预测前不得误判为行业 PEG 数据不足。
- 主包清除旧直接 API 和分页取数残留，统一为 `主 skill / references -> L2 分析原子 skill -> L1 查询原子 skill -> 数据服务` 的三层架构；`04/05/06` 只消费紧凑标准输出，不再维护查询参数。
- 严格收口用户可见与运行时文档中的旧高倍成长俗称，统一改写为“成长股特征”，并将原子 skill 更名为 `gs-growth-master-growth-feature-match`。
- 固定成长股特征综合评级公式：`符合=1分`、`较符合=0.5分`、`不符合=0分`、`数据不足=0分`，按 10 项状态确定高/中/低/数据不足。
- 固定行业景气度硬评分口径：只使用最近一期可得行业景气记录；个股模式按 `ROE趋势x/2 + 毛利率趋势x/1 + 净利率趋势x/1 + 机构评级x/1 = x/5` 计算，行业直问按 `ROE趋势x/2 + 毛利率趋势x/1 + 净利率趋势x/1 = x/4` 计算。
- 强化完整报告输出验收：完整/全面/深度请求必须逐段嵌入 `01-06` 各维度完整用户可见模板，不得只输出概括表。
- 将用户可见表达收口为“成长大师 / 成长投资方法论”，更新免责声明为“不构成任何投资建议”，并移除用户可见旧品牌表述。
- 将 PEG 输出从估值定性结论改为中性观察：展示 PE/PB/PEG、成长兑现压力、历史/行业相对水位、敏感性和适用边界。
- 将成长股特征模块从数字化主观评分改为定性匹配状态与综合评级，主观维度只保留证据说明和需验证事项。
- 收窄成长股特征模块枚举：单项状态固定为“符合 / 较符合 / 不符合 / 数据不足”，综合评级固定为“高 / 中 / 低 / 数据不足”，并统一输出核心匹配点、主要短板和需验证事项。
- 扩展研报查询标准输出 `source_refs`，预测依据和研报风险来源统一展示为“发布日期 + 研报标题 + 发布机构”，隐藏内部研报标识和原文定位信息。
- 收口查询 skill 内嵌后的分层口径：成长大师主流程和 references 只消费分析 skill、查询 skill 和研报查询 skill 的标准输出，不再维护底层接口补查表述。
- 清理误复制的 creator 标准模板 reference；行业分析中已有业务内容的输入、路由和输出校验文件改为业务化命名保留。
- 研报查询数据请求契约统一由主包 `references/data-reference/gs-research-report-query.md` 维护，并链接研报基础、特色和投资评级分组契约。
- 统一估值分析运行文档中的估值查询正式名为 `$gs-valuation-data`。
- 重构为综合业务交付场景结构：主 `SKILL.md` 只负责意图路由、总编排、输出风格和合规边界，可独立复用能力统一进入 `skills/`。
- 将六个行业/财务/估值分析与查询能力迁入 `skills/`，并将带空格目录规范化为 `gs-industry-analysis` / `gs-industry-data`。
- 新增 `references/gs-growth-master-growth-feature-match.md`，作为唯一成长大师专属成长股特征匹配原子 skill。
- 将六维流程和输出校验编号化为 `references/00-09`，能力圈、生命周期、景气度、盈利预测、PEG 和成长股特征流程均通过 reference 编排原子 skill。
- 明确不恢复旧 `skills/industry-lifecycle`、`skills/industry-prosperity`、`skills/earnings-forecast`、`skills/peg-valuation`、`skills/growth-feature-match` 薄包装子 skill。
- 调整轻量可读版成长大师表达：移除“成长生意”“难懂篮子”“传统能力圈”等巴菲特化残留，改为成长大师六类公司、成长线索、PEG 匹配、成长股特征和反证信号。
- 收敛企业盈利预测“未来预测”展示口径：将预测样本数、预测假设可靠性和增速口径合并为一行“预测口径”，减少 C 端流程化说明。
- 强化企业盈利预测未来预测表：已取得有效预测净利润时必须填入数值，样本分歧或可靠性不足只能写入解读，不得替代为占位文字。
- 规范企业盈利预测历史趋势表：同比列必须输出数字百分比，转亏、扭亏、亏损扩大或亏损收窄只作为括号说明。
- 强化轻量版 PEG 输出契约：涉及 PEG 或估值成长匹配时必须输出乐观 / 中性 / 悲观三情景 PEG 表格，并禁止用户可见输出点名具体数据平台、证券公司、研究机构或媒体名称。
- 收紧轻量版多情景 PEG 表格：必须包含 `vs 行业名称行业基准PEG` 列；行业基准 PEG 不足时保留列并标注“暂无足够数据”。
- 固定企业盈利预测未来净利润数据链路：未来 1-3 年预测净利润表格必须来自研报查询原子 skill 的预测评级标准输出，不再用历史增速、行业合理区间、用户假设或前序分析结论补填。
- 删除行业景气度“关键依据”表格中的 `标签` 列，关键依据仅保留指标、最新值和环比变化。
- 将行业数据与行业深度分析依赖从旧 skill 名迁移为 `gs-industry-data` / `gs-industry-analysis`，并同步工作区与 Codex 安装目录。
- 修复数据服务失败误判：新增全局数据可用性守门，禁止把单个接口失败、预测缺口、补充数据缺失或用户索要工程细节升级为“国信证券数据服务暂不可用”。
- 补强单维度行业景气度和 PEG 估值 reference：核心数据可得时必须输出有限业务分析，缺失项用业务化数据不足说明，不再直接阻断为全局失败。
- 补强复合请求路由：用户同时要求 PEG 分析和接口/字段/参数等工程明细时，先拒绝工程明细，再继续输出业务化 PEG 分析，不得返回数据服务不可用。
- 强化行业景气度表格约束：ROE、毛利率、净利率等核心字段已取得时必须填入表格，不得错误写成“数据不足”。
- 修复用户可见输出契约：完整报告和多标的报告必须输出非空一级标题，行业归类不再展示行业代码，行业景气度评分文案改为分项求和口径。
- 新增券商 APP 普通 C 端默认展示的“轻量可读版公司分析”模式，普通单股请求默认输出总分总结构。
- 调整单股分析路由：`single_stock_light_readable` 作为默认轻量版，`single_stock_full_flow` 仅在明确完整、全面、深度、查看完整分析、五维度或六维度分析时触发。
- 补强轻量版输出模板、核心判断固定枚举、三条核心理由、两个主要风险、成长大师式总结和“下一步可以继续了解”引导入口。
- 补充轻量版数据不足、框架不适配、风险高于优势和 PEG 成长匹配无法判断的兜底话术。
- 强化用户可见合规禁词，并将 PEG 与成长股特征 reference 中“持有”类旧模板文案改为非交易化研究表达。
- 修正成长股特征批量筛选的用户可见行业字段规则，内部一级行业代码仅用于取数和候选池构建，最终报告不输出 `industryCode`。
- 统一 references 内部相对路径写法，避免安装到 Codex skills 目录后仍引用上级 `../../references` 路径。
- 补强用户输出规范：即使 reference 保留接口、参数、字段或代码示例，最终用户可见输出也必须转写为业务语言。
- 接入 `gs-research-report-query` 研报查询原子 skill：研报预测样本、研报向量文本证据和研报观点风险统一消费该原子 skill 标准输出，不再在成长大师主流程中散落直接研报接口调用口径。
- 新增研报类缺失数据映射：行业渗透率、TAM/SAM、CR3/CR5、库存价格周期、ASP、产能供需、订单客户、研发认证、供应链和品牌渠道等缺口统一按研报查询 route 补充，并明确文本证据只作辅助、不并入硬评分。

## 4.0.0 - 2026-06-11

- 将主 `SKILL.md` 重构为成长大师思想大脑与 references 编排器，不再捆绑本地五个子模块。
- 将行业生命周期、行业景气度、企业盈利预测、PEG估值分析、成长股特征匹配迁入 `references/`，并保留原分析需求、评分规则、输出框架和成长大师方法论。
- 统一外部调用链路：reference 调用分析 skill，分析 skill 调用查询 skill，查询 skill 通过接口取数，分析 skill 返回结果后再由成长大师 reference 叠加方法论。
- 新增 `references/调用链路与数据补充.md`，明确查询 skill 未覆盖但成长大师框架必须使用的数据，按当前项目已有接口补充，最终用户输出不得暴露接口、参数、字段或脚本细节。
- 补充各维度的单维度调用规则：行业生命周期、行业景气度、企业盈利预测、PEG估值分析、成长股特征匹配均支持通过主 skill 独立调用，也支持完整六维度编排。
- 删除旧本地子模块目录与 `tests/`；本次重构不新增测试、不运行测试，仅做人工文件检查。

## 3.2.1 - 2026-06-09

- 新增行业直问范围澄清规则：跨行业主题、无法匹配公开行业分类或存在多个候选时，先追问细分行业，不直接生成行业生命周期和景气度分析。
- 首批跨行业主题覆盖 AI/人工智能、新能源、机器人，并给出通信、计算机、电子/半导体、传媒/AI应用、电力设备、汽车、有色金属、基础化工、公用事业、机械设备等候选方向。
- 优化 PEG 估值原子 skill 输出规则：结论摘要改为判断优先并只保留少量核心数据，具体数据集中到关键依据与情景测算。
- 要求 PEG 关键依据按“数据 + 含义”解释，不再只堆估值、增速和行业对比数字。
- 调整适用边界与成长大师估值视角：只输出与当前公司匹配的边界提醒，并将成长大师视角从固定模板改为单条个股化判断与解释。
- 保留适用边界和成长大师视角的候选参考库，供模型按个股情况选择和改写，避免缺少可用表达。

## 3.2.0 - 2026-06-08

- 调整行业数据调用链路：成长大师主 skill 编排两个行业原子 skill，行业原子 skill 优先通过 `gs-industry-data` 获取行业数据，`gs-finance-data` 仅作为保底。
- 轻量补强行业生命周期与行业景气度分析依据，允许使用行业估值、财务、盈利/景气、预测、行情、产业链、竞争结构和文本线索。
- 更新行业层级与输入分流参考文档，避免主 skill 或行业原子 skill 绕过 `gs-industry-data` 直接调用行业底层接口。
- 扩展静态检查，验证行业链路依赖声明与直接行业接口禁用规则。

## 3.1.0 - 2026-06-08

- 新增符合国信证券 skill 创建规范的框架章节：前置依赖、输入要求、意图路由、发布前评审。
- 在成长大师主 skill 和 5 个原子 skill 中同步 `metadata.clawdbot.requires.skills` 与 `requirements.skills`。
- 新增评审清单，用于检查 frontmatter、输入契约、意图路由、依赖声明、输出禁则和失败处理。
- 扩展静态检查脚本，验证新增框架项是否完整。
